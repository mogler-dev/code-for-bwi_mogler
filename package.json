{
  "name": "code-for-bwi",
  "version": "1.0.0",
  "description": "Benötigte Hardware, Kapazitäten und Nutzwerte",
  "main": "./src/app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "./node_modules/.bin/tsc -p tsconfig.json",
    "start": "npm run compile && node ./dist/app.js"
  },
  "author": "Christopher Mogler",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/node": "^14.14.20",
    "typescript": "^4.1.3"
  }
}
